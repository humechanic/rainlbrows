# Настройка автоматических напоминаний

Бот использует встроенный **JobQueue** из библиотеки `python-telegram-bot` для автоматической отправки напоминаний. Это означает, что напоминания работают автоматически при запуске бота - никаких дополнительных скриптов или cron не требуется!

## Как это работает

1. При запуске бота (`python main.py`) автоматически запускается JobQueue
2. JobQueue периодически (каждые 5 минут) проверяет базу данных
3. Находит пользователей, которым нужно отправить напоминание:
   - Первое напоминание: через 1 час после клика "забрать урок"
   - Второе напоминание: через 12 часов после клика
   - Напоминания об истечении предложения: за 24-48 часов до истечения
4. Отправляет напоминания через Telegram API

**Важно:** Каждый пользователь получает напоминание индивидуально, в зависимости от своего времени клика/старта, а не по общему расписанию.

## Запуск бота

Просто запустите бота как обычно:

```bash
source venv/bin/activate
cd app
python main.py
```

Напоминания будут работать автоматически! 🎉

## Настройка интервала проверки

По умолчанию проверка происходит каждые 5 минут. Чтобы изменить интервал, отредактируйте `app/main.py`:

```python
def setup_jobs(application):
    """Setup scheduled jobs for reminders"""
    job_queue = application.job_queue

    if job_queue is None:
        logging.warning("JobQueue is not available. Reminders will not work.")
        return

    # Измените interval на нужное значение (в секундах)
    job_queue.run_repeating(
        callback=process_reminders,
        interval=600,  # 10 минут = 600 секунд
        first=10,  # Начать через 10 секунд после запуска
        name='process_reminders'
    )
```

### Примеры интервалов:

- **Каждые 1 минута**: `interval=60`
- **Каждые 5 минут**: `interval=300` (по умолчанию)
- **Каждые 10 минут**: `interval=600`
- **Каждый час**: `interval=3600`

## Тестирование напоминаний

Для ручного тестирования используйте:

```bash
source venv/bin/activate
cd app
python test_reminders.py
```

Это запустит проверку напоминаний один раз без запуска всего бота.

## Проверка работы

### Проверить логи бота

Логи напоминаний будут отображаться в консоли при запуске бота:

```
INFO - Scheduled reminder processing: every 5 minutes
INFO - Found 1 users for first lead reminder
INFO - Sent first lead reminder to user_id=123456789
```

### Проверить в базе данных

```sql
-- Пользователи, которые кликнули урок
SELECT
    u.telegram_id,
    u.first_name,
    o.lesson_clicked_at,
    o.first_reminder_sent,
    o.second_reminder_sent,
    NOW() - o.lesson_clicked_at as time_since_click
FROM offers o
JOIN users u ON o.user_id = u.id
WHERE o.lesson_clicked_at IS NOT NULL
  AND o.is_active = true
ORDER BY o.lesson_clicked_at DESC;
```

## Преимущества встроенного JobQueue

✅ **Простота**: Не нужны дополнительные скрипты или cron  
✅ **Надежность**: Работает в том же процессе, что и бот  
✅ **Автоматический перезапуск**: Если бот перезапускается, напоминания продолжают работать  
✅ **Интеграция**: Полный доступ к контексту бота и его функциям  
✅ **Нет зависимостей**: Использует только стандартные возможности python-telegram-bot

## Устранение проблем

### Напоминания не отправляются

1. **Проверьте, что бот запущен**

   ```bash
   ps aux | grep "python main.py"
   ```

2. **Проверьте логи бота** - должны быть сообщения о запуске JobQueue:

   ```
   INFO - Scheduled reminder processing: every 5 minutes
   ```

3. **Проверьте базу данных** - есть ли пользователи с `lesson_clicked_at`:

   ```sql
   SELECT COUNT(*) FROM offers WHERE lesson_clicked_at IS NOT NULL;
   ```

4. **Проверьте, что прошло достаточно времени**:
   - Первое напоминание: через 1 час после клика
   - Второе напоминание: через 12 часов после клика

### JobQueue не запускается

Если вы видите предупреждение:

```
WARNING - JobQueue is not available. Reminders will not work.
```

Это означает, что JobQueue не инициализирован. Убедитесь, что используете правильную версию `python-telegram-bot` (22.5+).

### Ошибки подключения к базе данных

1. Проверьте, что PostgreSQL запущен
2. Проверьте `DATABASE_URL` в `app/env.py`
3. Проверьте права доступа к базе данных

## Дополнительная информация

Документация по JobQueue: https://docs.python-telegram-bot.org/en/stable/telegram.ext.jobqueue.html
