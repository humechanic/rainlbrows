# Быстрый старт: Автоматические напоминания

## 🎉 Напоминания работают автоматически!

Бот использует встроенный **JobQueue** из `python-telegram-bot`. Просто запустите бота - напоминания будут работать автоматически!

## Запуск

```bash
source venv/bin/activate
cd app
python main.py
```

Всё! Напоминания будут проверяться каждые 5 минут автоматически. 🚀

## Как это работает?

1. Бот запускается и инициализирует JobQueue
2. Каждые 5 минут проверяется база данных
3. Находятся пользователи, которым нужно отправить напоминание
4. Напоминания отправляются автоматически

**Каждый пользователь получает напоминание индивидуально**, в зависимости от своего времени клика/старта.

## Тестирование

Для ручного тестирования:

```bash
source venv/bin/activate
cd app
python test_reminders.py
```

## Изменение интервала

Отредактируйте `app/main.py`, функция `setup_jobs()`:

```python
job_queue.run_repeating(
    callback=process_reminders,
    interval=600,  # 10 минут (в секундах)
    ...
)
```

## Проверка работы

Логи напоминаний отображаются в консоли:
```
INFO - Scheduled reminder processing: every 5 minutes
INFO - Found 1 users for first lead reminder
INFO - Sent first lead reminder to user_id=123456789
```

Подробнее: `SCHEDULER_SETUP.md`
